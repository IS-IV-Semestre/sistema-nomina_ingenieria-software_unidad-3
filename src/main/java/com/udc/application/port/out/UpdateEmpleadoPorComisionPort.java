package com.udc.application.port.out;

import com.udc.domain.models.empleado.EmpleadoPorComision;

public interface UpdateEmpleadoPorComisionPort {
    EmpleadoPorComision update(EmpleadoPorComision empleadoPorComision);
}
